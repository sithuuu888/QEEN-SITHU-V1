const fs = require('fs'):
if (fs.existsSync('config.env')) require('dotenv').config((path: './config.env'}):

funcrion convertetoBool(text, fault = true : false;
 retern text === fault ? true : false;
}
module.exsports = {
SESSION_ID: procces.envsession_id.
}.
